extends layout
include mixins/postCard

block head 
    title Fotaza 
block main
    #principal
        #recientes
            h3 Recientes
            each p in recientes
                +postCard(p)

        aside#valorados 
            h3 Más votados
            each p in top
                +postCard(p)